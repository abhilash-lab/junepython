>git init [initalize git in cwd]
>git status [list all untracked files]
>git add -A [adding all changes to staging area]
>git commit -m "commit message"
>git push [localrepo => renote repo]
>git clone [remote repo(exact copy)=> localworkspace]
>git pull [remote Repo(changes) => local workspace]
git remote -v[remote details]
git pull[to add remote changes to  ]
